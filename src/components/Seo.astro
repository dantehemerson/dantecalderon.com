---
import { getImage } from 'astro:assets'

export interface Props {
  title?: string
  description?: string
  image?: ImageMetadata
  url?: string
  isPost?: boolean
  date?: string | Date
}

const {
  title = 'Dante Calderon',
  description = 'Dante Calderon - Backend Node.js Developer',
  image,
  url = 'https://dantecalderon.com',
  isPost = false,
  date,
} = Astro.props

// Create image for OpenGraph. Aspect Ratio: 1.91:1
const optimizedOpenGraphImage = image
  ? await getImage({
      src: image,
      width: 1200,
      height: 630,
      format: 'jpeg', // JPEGs are widely supported by OG parsers
      fit: 'cover',
      position: 'center',
    })
  : null

const siteName = 'Dante Calderon'
const fullTitle = title === siteName ? title : `${title} · ${siteName}`

const openGraphImageUrl =
  optimizedOpenGraphImage?.src ?? image?.src ?? 'https://dantecalderon.com/logo.png'

const publishedDate = date instanceof Date ? date.toISOString() : date

const schema: any = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Person',
      '@id': 'https://dantecalderon.com#person',
      name: 'Dante Calderon',
      url: 'https://dantecalderon.com',
      sameAs: [
        'https://twitter.com/dantehemerson',
        'https://github.com/dantehemerson',
        'https://www.linkedin.com/in/dantecalderon',
      ],
    },
    {
      '@type': 'WebSite',
      '@id': 'https://dantecalderon.com#website',
      url: 'https://dantecalderon.com',
      name: siteName,
      author: { '@id': 'https://dantecalderon.com#person' },
    },
  ],
}

if (isPost) {
  schema['@graph'].push({
    '@type': 'BlogPosting',
    '@id': url + '#article',
    mainEntityOfPage: url,
    headline: title,
    name: title,
    description,
    image: { '@type': 'ImageObject', url: openGraphImageUrl },
    ...(publishedDate && { datePublished: publishedDate }),
    author: { '@id': 'https://dantecalderon.com#person' },
  })
}
---

<title>{fullTitle}</title>
<meta name="description" content={description} />
<meta name="image" content={openGraphImageUrl} />
<meta name="distribution" content="global" />

<link rel="canonical" href={url} />

<meta property="og:url" content={url} />
<meta property="og:type" content={isPost ? 'article' : 'website'} />
<meta property="og:title" content={fullTitle} />
<meta property="og:description" content={description} />
<meta property="og:image" content={openGraphImageUrl} />

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:site" content="@dantehemerson" />
<meta name="twitter:creator" content="@dantehemerson" />
<meta name="twitter:title" content={fullTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={openGraphImageUrl} />

<script is:inline type="application/ld+json" set:html={JSON.stringify(schema)} />
